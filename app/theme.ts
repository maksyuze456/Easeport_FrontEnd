// Minimal Mantine theme fallback used when migrating projects.
// You can expand this object to customize colors, fonts, spacing, etc.

export const theme = {
  colorScheme: 'light',
  // examples:
  // fontFamily: 'Inter, system-ui, -apple-system, sans-serif',
};
